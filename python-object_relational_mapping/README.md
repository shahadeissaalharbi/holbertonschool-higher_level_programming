# Python - Object Relational Mapping

This project introduces Object Relational Mapping (ORM) concepts by connecting
Python scripts to a MySQL database using the `MySQLdb` module.

## Tasks

### 0. Get all states
`0-select_states.py` connects to a MySQL server and lists all rows from the
`states` table of a given database, sorted in ascending order by `id`.

**Usage:**
